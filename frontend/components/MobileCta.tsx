/** Removed: the WhatsApp floating button and the header's Book Now cover this.
 *  Kept as an empty module only so a stale dev-server build can resolve it;
 *  safe to delete once the dev server has been restarted. */
export default function MobileCta() {
  return null;
}
